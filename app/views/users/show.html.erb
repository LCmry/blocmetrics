<div class="container">
  <div class="row">
    <div class="col-md-4">
      <h2> Your Sites: </h2>
      <% @sites.each do |site| %>
        <h3><%= link_to site.name, [@user, site] %></h3>
      <% end %>
      <%= link_to "New Site", new_user_site_path(@user, @site) %>
    </div>
  </div>
</div>